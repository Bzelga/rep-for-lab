query GetDevelNextPage($countItem:Int!, $cursot: String!){
  allDeveloper(first: $countItem, after:$cursot){
    nodes{
      ...DevelopersInfo
    }
    pageInfo{
      hasNextPage
      endCursor
    }
  }
}