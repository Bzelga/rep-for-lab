query GetDevelPrevPage($countItem:Int!, $cursot: String!){
  allDeveloper(last: $countItem, before:$cursot){
    nodes{
      ...DevelopersInfo
    }
    pageInfo{
      hasNextPage
      endCursor
    }
  }
}